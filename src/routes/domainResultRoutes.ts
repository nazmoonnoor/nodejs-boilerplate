// Dependencies
import express from "express";

const router = express.Router();

// router.get("/", use(memberController.downloadAbsences));
// router.get("/absences", use(memberController.getAbsences));

export default router;
